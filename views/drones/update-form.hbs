<h2>Update {{drones.name}}</h2>
<form action="/drones/{{drones.id}}/edit" method="POST">
    <label>Name</label>
    <input type="text" name="name" value="{{drones.name}}">

    <label>Propeller</label>
    <input type="number" name="propeller" value={{drones.propellers}}>

    <label>maxSpeed</label>
    <input type="number" name="maxSpeed" value={{drones.maxSpeed}}>

    <button type="submit">Update</button>
</form>